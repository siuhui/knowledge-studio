# 迭代文档目录

本目录用于记录按版本/迭代的执行文档，不作为长期事实源。

## 当前执行版本
- `v0.1`（开发态，未上线）

## 目标上线版本
- `v1.0`（GA，待达到发布门禁）

## 结构约定
- `docs/iterations/<version>/README.md`
- `docs/iterations/<version>/sprint-xx-plan.md`
- `docs/iterations/<version>/sprint-xx-review.md`
- `docs/iterations/<version>/rfc-*.md`（可选）

## 回写规则
- 迭代结束前，将最终设计与结论回写到 `docs/features/`。
- 发布时在 `docs/releases/` 做版本映射与结果归档。
